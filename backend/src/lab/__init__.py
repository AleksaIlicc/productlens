"""Temporary scraping lab: internet search + scraping for a product name.

Self-contained on purpose. Nothing here calls an LLM; the point is to prepare
clean offers (`LabPayload`) that the LLM comparison step can consume later.
"""
